package com.obvious.nasagalleryapp.core.constants

object AppConstants {

    const val DEFAULT_PADDING = 16

    const val DEFAULT_GRID_COLUMNS = 2
    const val DEFAULT_GRID_HEIGHT = 200

    const val KEY_SELECTED_URL = "selected_url"
}